<%= form_for(@historias) do |f| %>
  <% if @historias.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historias.errors.count, "error") %> prohibited this historias from being saved:</h2>

      <ul>
      <% @historias.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_singularidad %><br>
    <%= f.number_field :id_singularidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
